# import logging

# from src.environment import env
 
# path = f"storage/logs/{env('APP_NAME')}.log"
# max_size = env('LOG_MAX_SIZE', 1000000)
# backup_count = env('LOG_BACKUP_COUNT', 5)
# level = env('LOG_LEVEL', logging.INFO)
# format = env('LOG_FORMAT', '%(asctime)s - %(name)s - %(levelname)s - %(message)s')
