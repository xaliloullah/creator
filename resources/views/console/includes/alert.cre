from main import Creator 


if Creator.session.has_errors():
    for error in Creator.session.get_errors(): 
        Creator.terminal.error(error)
        
if Creator.session.has_success():
    for msg in Creator.session.get_success(): 
        Creator.terminal.success(msg) 
        
        
