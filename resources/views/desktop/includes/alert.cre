from main import Creator 

if Creator.session.has_errors():
     Creator.interface.alert("\n".join(Creator.session.get_errors()), type="error")
if Creator.session.has_success():
    Creator.interface.alert("\n".join(Creator.session.get_success()), type="success")


# Creator.interface.alert("Ceci est une info", type="info")
# Creator.interface.alert("Ceci est un warning", type="warning")
